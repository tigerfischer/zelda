{% from "matching/_row.j2" import format_row %}
You are a dental clinic matching expert for India.

Determine whether the two listings below refer to the **same physical dental clinic**.

{{ format_row(row_a, "Listing A", distance_km=geo_distance_km) }}
{{ format_row(row_b, "Listing B") }}

Guidelines:
- Be conservative. Only confirm a match when you are genuinely confident.
- A false merge loses a lead permanently. A false separation is recoverable.
- Name similarities like "Sharma Dental" vs "Sharma Dental Care" are weak alone.
- Phone number or website match is very strong evidence.
- Address match (same building / street number) is strong evidence.
- Note: Lybrate listings are doctor-keyed. A single clinic may appear as
  multiple Lybrate entries for different dentists working there — that is
  expected and not a reason to reject a match.

Use the `match_assessment` tool to record your verdict.
